# Caddy автоматически получает и продлевает TLS-сертификат Let's Encrypt для {$DOMAIN}.
# Требуется, чтобы A-запись домена уже указывала на IP этого сервера и порты 80/443 были открыты.
{$DOMAIN} {
	encode gzip zstd
	reverse_proxy app:3000
}
